package AdvMath;

public class TrigAlgo {
    // Calculates y = sin(x) + cos(x) + tan(x)
    public double calculateComplexTrig(double radians) {
        // Fun Fact: 'Tangent' means 'touching' in Latin.
        return Math.sin(radians) + Math.cos(radians) + Math.tan(radians);
    }
}
